# FW-NNNN - Short title

- Status: planned
- Disposition: unexecuted
- Date: YYYY-MM-DD
- Owner:
- Parent experiments: none
- Exactness: L0/L1/L2/L3/L4/L5
- Hardware/runtime: Apple M1 Mac mini, 16 GiB, internal SSD

## Question and hypothesis

State the falsifiable question, expected causal mechanism, and why this is the
cheapest useful test.

## Frozen authority and baseline

Record checkpoint, source, fixture, commit, toolchain, configuration, and
baseline identities. State dirty/clean status and protocol deviations.

## Method and commands

Record exact commands, workload slices, cold/warm state, batch size,
concurrency, accepted-token accounting, `A`, `U`, bytes, and safety boundaries.

## Gates

Define correctness, performance, safety, continuation, and kill thresholds
before execution. State excluded claims.

## Result

Preserve every run, evidence manifest or content hash, correctness result,
complete-path result, diagnostic attribution, and unfavorable observation.

## Decision

Assign one disposition from `docs/WORKFLOW.md`, state confidence and
limitations, name the promoted or killed branch, and link superseded or
reversing evidence. Update `LEARNINGS.md` and `spec/throughput-model.json` when
the result changes a belief or measured constant.
